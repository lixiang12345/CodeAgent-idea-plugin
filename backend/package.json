{
  "name": "codeagent-backend",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.mjs",
    "test": "node --test test/**/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
